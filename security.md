# Security

The MVP defaults to simulation/demo behavior. The policy engine is deterministic and rejects unsupported chains/protocols, unverified contracts, insufficient liquidity, excessive slippage, and concentration above the configured limit. Transactions above the autonomous threshold require confirmation.
