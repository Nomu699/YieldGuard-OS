# Threat model

Key threats: prompt injection, malicious contracts, stale yield data, excessive concentration, slippage, unauthorized signing, and incorrect transaction verification.

Mitigations: allowlists, deterministic policy checks, explicit confirmation, simulation, contract verification, and post-transaction verification. Production deployment requires audited adapters and key-management controls.
